module Guards where

-- Return a letter grade for a score:
--   score >= 90 -> 'A'
--   score >= 80 -> 'B'
--   score >= 70 -> 'C'
--   otherwise   -> 'F'
gradeLetter :: Int -> Char
gradeLetter score = undefined

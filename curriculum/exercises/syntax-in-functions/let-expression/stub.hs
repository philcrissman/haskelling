module LetExpression where

-- For a w-by-h box, compute area = w * h and perimeter = 2 * (w + h),
-- then return area + perimeter.  e.g. boxMetric 3 4 = 12 + 14 = 26
boxMetric :: Int -> Int -> Int
boxMetric w h = undefined

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: haskelling_dev
      POSTGRES_USER: haskelling
      POSTGRES_PASSWORD: haskelling
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:

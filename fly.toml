app = "haskelling-app"
primary_region = "sjc"

[build]
  dockerfile = "backend/Dockerfile"
  # Build context is the repo root so CURRICULUM.json is accessible.
  # docker build -f backend/Dockerfile -t haskelling .

[http_service]
  internal_port = 8080
  force_https = true
  # Pre-launch: stop the machine when idle so it isn't billed (and so the
  # Neon compute can autosuspend) — auto_start brings it back on the next
  # request. Set min_machines_running = 1 at launch if cold starts matter.
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

  [http_service.concurrency]
    type = "requests"
    hard_limit = 200
    soft_limit = 100

[[http_service.checks]]
  grace_period = "10s"
  interval = "60s"
  method = "GET"
  path = "/health"
  timeout = "5s"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
